<h1>Latests Postings</h1>
<% @posts.each do |post| %>
    <div class="well">
        <h3><%= post.title %></h3>
        <p><%= post.body %></p>
        <%= link_to 'More...', post_path(post), :class => 'btn btn-info' %>
    </div>
<% end %>
